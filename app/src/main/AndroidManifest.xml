<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".Notes_Main"
            android:exported="false"
            android:label="@string/title_activity_notes_main"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".Exercise_Main"
            android:exported="false"
            android:label="@string/title_activity_exercise_main"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".BottmonNav"
            android:exported="true"
            android:label="@string/title_activity_bottmon_nav" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>